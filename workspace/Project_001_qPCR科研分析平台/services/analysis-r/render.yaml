services:
  - type: web
    name: qpcr-analysis-r
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: ANALYSIS_R_SHARED_SECRET
        generateValue: true
      - key: ALLOWED_ORIGINS
        sync: false
